//! Handles the `binaryify` operation.

use crate::cli::{clap::BinaryifyArguments, types::CliOpError};

pub(super) fn binaryify(args: &BinaryifyArguments) -> Result<(), CliOpError> {
    _ = args;
    todo!();
}
